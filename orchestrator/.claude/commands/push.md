---
description: Git add, commit, and push changes to the remote repository
allowed-tools: Bash(git status:*), Bash(git diff:*), Bash(git add:*), Bash(git commit:*), Bash(git push:*)
---

I'll add all changes, create a commit message based on the changes, and push to the remote repository without appending my identity.
